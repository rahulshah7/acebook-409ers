<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Acebook", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="ml-auto navbar-nav">
      <li class="nav-item">
        <% if user_signed_in? %>
          <%= link_to "My Posts", user_posts_path(current_user.id), class: "nav-link" %>
        <% else %>
          <%= link_to "My Posts", root_path, class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Account
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <p class="navbar navbar-light">Hello, @<%= current_user.email.split('@').first %>!</p>
          <% else %>
            <p class="navbar navbar-light">Hello future Acebooker!</p>
          <% end %>
          <div class="dropdown-divider"></div>
          <%= link_to "Account Settings", edit_user_registration_path, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <% if user_signed_in? %>
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "dropdown-item" %> 
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
